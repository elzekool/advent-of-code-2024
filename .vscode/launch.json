{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch current script",
            "program": "${file}",
            "cwd": "${fileDirname}"
        }
    ]
}